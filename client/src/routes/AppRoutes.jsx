import { Routes, Route } from 'react-router-dom';

export const AppRoutes = () => {
    return (
        <Routes>
            <Route path="/" element={<div>Admin Dashboard Home Placeholder</div>} />
        </Routes>
    );
};
